Aggregate all .nondex results in the same folder

find . -type d -iname '.nondex' -exec cp -r {} /home/alex/Desktop/abc \;


To find all the modules with failures within the aggregated folder

find . -type f -name "failures" -size +0 -exec grep -q . {} \; -print
